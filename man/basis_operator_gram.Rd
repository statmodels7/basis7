% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/numerical_fallbacks.R
\name{basis_operator_gram}
\alias{basis_operator_gram}
\title{The Roughness Matrix of a Differential Operator}
\usage{
basis_operator_gram(basis, op, at = NULL, weight = NULL, ...)
}
\arguments{
\item{basis}{A \link{basis}.}

\item{op}{A \link{LinearOperator}, with its period resolved.}

\item{at}{The covariate values, for the empirical measure, or \code{NULL}.}

\item{weight}{A density to integrate against, or \code{NULL}.}

\item{...}{Passed to methods, and on to the quadrature (\code{panels},
\code{nodes}).}
}
\value{
A symmetric numeric matrix of \code{basis@dimension} rows and columns.
}
\description{
The Gram matrix of \eqn{Lb}, that is
\deqn{R = \int_a^b (Lb)(Lb)^\top \, \mathrm{d}\mu,}
the matrix for which \eqn{\lVert Lx \rVert^2 = c^\top R c} when
\eqn{x = b^\top c}. It is what \code{\link[=basis_gram]{basis_gram()}} returns when its \code{order} is
a \link{LinearOperator}, and the generic exists so that a family with a closed
form can declare one.
}
\details{
The base method integrates numerically, evaluating \eqn{Lb} through
\code{\link[=operator_eval]{operator_eval()}} at Gauss-Legendre nodes, at the covariate values for the
empirical measure, or against a weight function. A \link{FourierBasis} over a
full period overrides it with an exact diagonal form; see
\code{\link[=basis_operator_gram.FourierBasis]{basis_operator_gram.FourierBasis()}}.
}
\examples{
b <- fourier_basis(lower = 0, upper = 365, dimension = 7)
round(diag(basis_gram(b, order = harmonic_operator(365))), 6)
}
\seealso{
\code{\link[=basis_gram]{basis_gram()}}, the generic that routes to it;
\code{\link[=operator_eval]{operator_eval()}} for \eqn{Lb}.
}
\keyword{internal}
