% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poly_basis.R
\name{PolyBasis}
\alias{PolyBasis}
\title{Legendre Polynomial Basis}
\usage{
PolyBasis(
  basis_name = character(0),
  dimension = integer(0),
  lower = integer(0),
  upper = integer(0),
  basis_params = list()
)
}
\arguments{
\item{basis_name}{A single string naming the family, printed by
\code{\link[=print.basis]{print.basis()}} and used by wrappers to build their own name. Not read by
any computation.}

\item{dimension}{The number of functions in the basis, a single integer of
at least 1. Must be of storage mode integer.}

\item{lower, upper}{The endpoints of the interval the basis lives on, or one
endpoint per variable for a basis of several. Both finite, of the same
length, and \code{lower[j] < upper[j]} for every \code{j}. Their length is what
\code{\link[=basis_nvar]{basis_nvar()}} reports.}

\item{basis_params}{A named list of whatever else the subclass needs: the
knots and degree of a B-spline, the frequency of a Fourier basis, the
marginal dimensions of a product. \code{\link[=print.basis]{print.basis()}} shows it, abbreviating
any numeric entry of more than four values. Defaults to an empty list.}
}
\value{
An object of class \code{PolyBasis}, inheriting from \link{basis}, with the
same five properties and \code{basis_params} holding \code{degree}. Call
\code{\link[=poly_basis]{poly_basis()}} instead of this class directly; it validates its arguments
and fills in \code{basis_name} and \code{basis_params}.
}
\description{
The S7 class of Legendre polynomial bases, the objects \code{\link[=poly_basis]{poly_basis()}}
returns. It adds no property to \link{basis} and exists as the class the
Legendre methods dispatch on: every one of \code{\link[=basis_eval]{basis_eval()}},
\code{\link[=basis_deriv]{basis_deriv()}}, \code{\link[=basis_int]{basis_int()}}, \code{\link[=basis_gram]{basis_gram()}} and \code{\link[=basis_colnames]{basis_colnames()}} has a
closed-form method registered here, so nothing about a Legendre basis is
computed by finite differences.
}
\section{Where the numbers come from}{
The polynomials are generated by the three-term recurrence
\deqn{(n+1) P_{n+1}(t) = (2n+1)\, t\, P_n(t) - n\, P_{n-1}(t),}
on \eqn{t \in [-1, 1]}, with \eqn{P_0 = 1} and \eqn{P_1 = t}, and the
variable shifted from the basis interval by
\eqn{t = 2(x - \ell)/(u - \ell) - 1}.

Derivatives come from the same recurrence differentiated,
\eqn{P_{n+1}^{(d)} = P_{n-1}^{(d)} + (2n+1) P_n^{(d-1)}}, so every order is
exact and no order is a special case; see \code{\link[=legendre_deriv]{legendre_deriv()}}. The integral
uses \eqn{\int P_n = (P_{n+1} - P_{n-1})/(2n+1)} and is exact as well.
}

\section{Its \code{basis_params}}{
One entry, \code{degree}, equal to \code{dimension - 1}, the highest degree the basis
carries. Nothing else is stored: the polynomials are determined by the
interval and the count.
}

\examples{
p <- poly_basis(dimension = 4)
S7::S7_inherits(p, PolyBasis)
p@basis_params$degree

# No component of a Legendre basis is numerical.
basis_is_numerical(p)

}
\seealso{
\code{\link[=poly_basis]{poly_basis()}}, the constructor;
\code{\link[=basis_eval.PolyBasis]{basis_eval.PolyBasis()}}, \code{\link[=basis_deriv.PolyBasis]{basis_deriv.PolyBasis()}},
\code{\link[=basis_int.PolyBasis]{basis_int.PolyBasis()}} and \code{\link[=basis_gram.PolyBasis]{basis_gram.PolyBasis()}} for the methods
registered on it.
}
