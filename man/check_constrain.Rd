% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoother.R
\name{check_constrain}
\alias{check_constrain}
\title{Check a Polynomial Family's Constraint Argument}
\usage{
check_constrain(constrain, op)
}
\arguments{
\item{constrain}{The value given, \code{NULL} or a whole number.}

\item{op}{The penalty's operator.}
}
\value{
\code{constrain} as a length-one integer, or \code{NULL}.
}
\description{
Validates \code{constrain} for a family whose null space is the polynomials of
degree below \code{order}. The constraint must \strong{contain} that null space, so
a degree below \code{order - 1} is rejected.
}
\details{
The requirement is not a convention. A direction the penalty does not see
and the constraint does not remove is neither penalized nor identified,
and \code{\link[=dr_basis]{dr_basis()}} signals an error there: with \code{order = 3} and a constraint
spanning only the constant and the linear function, the quadratic
direction is left free and unpenalized. Reported here, where the two
numbers were written, rather than several frames down.

Constraining \strong{beyond} the null space is legitimate. It buys
orthogonality to a parametric term written in the formula. Measured on
\code{y ~ 1 + x + x^2 + s(x)} at \code{k = 20} and 400 observations, the largest
correlation between a column of the block and \code{x^2} falls from 0.995 at
the default to 1.9e-15 at \code{constrain = 2}, which is exact by
construction, and the standard error of the quadratic coefficient falls
with it by more than an order of magnitude. The cost is one dimension.
}
\keyword{internal}
