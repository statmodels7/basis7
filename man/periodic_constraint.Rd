% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoother_families.R
\name{periodic_constraint}
\alias{periodic_constraint}
\title{The Periodicity Constraint of a Spline Basis}
\usage{
periodic_constraint(basis, degree)
}
\arguments{
\item{basis}{The parent basis, of dimension \code{k + degree}.}

\item{degree}{The degree of the spline, one row per derivative order below
it.}
}
\value{
A numeric matrix with \code{degree} rows and \code{basis@dimension} columns.
}
\description{
Returns the matrix whose rows say that a function of \code{basis} and its first
\code{degree - 1} derivatives take the same value at the two ends of the
interval: row \eqn{j} is \eqn{B^{(j)}(a) - B^{(j)}(b)} for
\eqn{j = 0, \ldots, d - 1}. Its null space is the periodic splines, which
is what \code{\link[=cyclic_smooth]{cyclic_smooth()}} builds on.
}
\details{
The rows are read from \code{\link[=basis_deriv]{basis_deriv()}} at the two endpoints, order zero
included, so the constraint is evaluated by the same arithmetic that
evaluates the basis rather than from a formula about knots.
}
\keyword{internal}
