% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator.R
\name{linear_operator}
\alias{linear_operator}
\title{A Linear Differential Operator From Its Weights}
\usage{
linear_operator(w)
}
\arguments{
\item{w}{The weights \eqn{w_0, \ldots, w_{m-1}}, a numeric vector of at
least one finite entry. Its length is the order of the operator.}
}
\value{
An S7 object of class \link{LinearOperator}.
}
\description{
The general form: \code{linear_operator(w)} is
\eqn{L x = w_1 x + w_2 Dx + \cdots + w_m D^{m-1} x + D^m x}, with the
leading coefficient 1 and \code{w} given in increasing order of
differentiation. It is the vector \code{fda::vec2Lfd()} takes.
}
\details{
The null space is read from the roots of the characteristic polynomial
\eqn{r^m + w_{m-1} r^{m-1} + \cdots + w_0}: a real root \eqn{a} of
multiplicity \eqn{\mu} contributes \eqn{t^i e^{at}} for \eqn{i < \mu}, and
a complex pair \eqn{a \pm bi} contributes \eqn{t^i e^{at}\cos(bt)} and
\eqn{t^i e^{at}\sin(bt)}. \code{\link[=operator_null]{operator_null()}} reports it.

Use \code{\link[=deriv_operator]{deriv_operator()}} and \code{\link[=harmonic_operator]{harmonic_operator()}} where they apply: they
say what the operator is for, and they carry a period that can be
resolved from a smoother's interval, which a bare weight vector cannot.
}
\examples{
# the harmonic acceleration operator written out, as in fda
nu2 <- (2 * pi / 365)^2
linear_operator(c(0, nu2, 0))

# and it is the same operator harmonic_operator() builds
all.equal(operator_weights(linear_operator(c(0, nu2, 0))),
          operator_weights(harmonic_operator(365)))

# exponential growth left unpenalized: L x = Dx - r x
operator_null(linear_operator(-0.5))
}
\seealso{
\code{\link[=deriv_operator]{deriv_operator()}} and \code{\link[=harmonic_operator]{harmonic_operator()}} for the named
instances, \code{\link[=operator_null]{operator_null()}} for the null space.
}
