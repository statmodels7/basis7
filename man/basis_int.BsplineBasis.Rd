% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bspline_basis.R
\name{basis_int.BsplineBasis}
\alias{basis_int.BsplineBasis}
\title{Integral of a B-Spline Basis}
\arguments{
\item{basis}{A \link{BsplineBasis} object.}

\item{x}{A numeric vector of evaluation points inside the basis interval.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric matrix with \code{length(x)} rows and \code{basis@dimension}
columns, exactly zero in the row at \code{basis@lower}.
}
\description{
Returns \eqn{\int_{\ell}^{x} B_j(t)\,\mathrm{d}t} for every function,
exactly, from \code{\link[splines2:bSpline]{splines2::bSpline()}} with \code{integral = TRUE}. That function
anchors its integral at the lower boundary knot, which is the same
convention \code{\link[=basis_int]{basis_int()}} states, so no correction is applied here.
}
\details{
The integral of a spline of degree \eqn{m} is a spline of degree
\eqn{m + 1} on the same knots, and \pkg{splines2} evaluates it from the
recurrence, with no quadrature anywhere.

The row at \code{basis@upper} holds the area under each function. Because the
basis is a partition of unity, those areas sum to the width of the
interval, which is a cheap check that the two conventions agree.
}
\seealso{
\code{\link[=basis_int]{basis_int()}} for the generic and the anchoring convention;
\code{\link[=bspline_design]{bspline_design()}}, the one call into \pkg{splines2}.
}
\keyword{internal}
