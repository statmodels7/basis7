% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoother_families.R
\name{fourier_smooth}
\alias{fourier_smooth}
\title{A Fourier Smoother}
\usage{
fourier_smooth(
  k = 9,
  order = 2,
  measure = "lebesgue",
  null_space = "keep",
  reparam = "dr",
  penalty = NULL,
  omega = NULL,
  lower = NULL,
  upper = NULL
)
}
\arguments{
\item{k}{The number of basis functions, an \strong{odd} whole number of at
least 3: a Fourier basis holds a constant plus complete sine-cosine
pairs.}

\item{order}{The order of derivative the penalty integrates.}

\item{measure}{The measure the roughness is integrated against.}

\item{null_space}{What becomes of the null space. It is the constant
alone here, and the constant is removed whatever this says, so the
argument is accepted for symmetry with the other families and changes
nothing.}

\item{reparam}{The coordinates the coefficients live in.}

\item{penalty}{\code{NULL} for the quadratic roughness penalty, or a factory
building a penalty from a coefficient count. See the section on the
smoother's own page.}

\item{omega}{The period, \code{NULL} for the width of the interval.}

\item{lower, upper}{The interval, which for a periodic basis is the period.
See the section above: give both.}
}
\value{
An S7 object of class \link{FourierSmoother}, inheriting from
\link{smoother}.
}
\description{
The periodic smoother: \code{k} Fourier functions over an interval, penalized
by the integrated squared derivative of order \code{order}, rotated to the
Demmler-Reinsch coordinates. Every column of the block is periodic, so a
fit built on it takes the same value at the two ends of the interval.
}
\section{What it does not have, and why}{
\code{degree} is a B-spline's, and a Fourier basis has none.

\code{constrain} in the form "the polynomials up to degree c" has no reading
here: a periodic basis contains no linear function, so its penalty's null
space is the \strong{constant at every order}, not a space growing with the
order. Measured on the Gram matrix of a nine-function Fourier basis at
orders 1, 2 and 3, the null function has a standard deviation of exactly
zero, which is to say it is constant, and the null space is
one-dimensional in all three.

The constant is removed, so a model carrying an intercept spans the level
and the smooth carries the shape. Nothing is restored as a free column,
which is why \code{null_space} has no effect here and \code{k} functions give
\code{k - 1} columns.
}

\section{The interval is the period}{
A periodic basis needs its interval fixed by the modeller, not read from
the data: the period is a fact about the covariate, and the range of one
sample is not it. Give \code{lower} and \code{upper}, as in
\code{fourier_smooth(k = 9, lower = 0, upper = 365)} for a day of the year.
Without them the interval is the padded range of the data, which makes
the basis periodic on an interval nothing else knows about.
}

\examples{
# A periodic covariate, and a truth that is periodic on [0, 1].
set.seed(3)
x <- sort(runif(300))
f <- function(u) sin(2 * pi * u) + 0.4 * cos(4 * pi * u)
y <- f(x) + rnorm(300, sd = 0.2)

sm <- fourier_smooth(k = 9, lower = 0, upper = 1)
out <- smoother_build(sm, x)
dim(out$X)

# THE FIT IS PERIODIC, which is the property the basis was chosen for.
b <- solve(crossprod(out$X) + 0.01 * out$S, crossprod(out$X, y))
ends <- smoother_apply(sm, out$blueprint, c(0, 1)) \%*\% b
format(diff(as.vector(ends)), digits = 3)
}
\seealso{
\code{\link[=smoother_build]{smoother_build()}} for what it produces at data,
\code{\link[=bspline_smooth]{bspline_smooth()}} for the non-periodic family, \code{\link[=fourier_basis]{fourier_basis()}} for
the basis alone.
}
