% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator.R
\name{operator_resolve}
\alias{operator_resolve}
\title{Fill In an Operator's Period From an Interval}
\usage{
operator_resolve(op, lower, upper)
}
\arguments{
\item{op}{A \link{LinearOperator}.}

\item{lower, upper}{The interval, two numbers.}
}
\value{
An S7 object of class \link{LinearOperator}, with numeric weights.
}
\description{
Returns the operator with its period set to the width of \verb{[lower, upper]}
where it was left \code{NULL}, and unchanged where it was given. It is what a
smoother calls before building its penalty, so that
\code{fourier_smooth(lower = 0, upper = 365)} penalizes on a cycle of 365
without the period having to be written twice.
}
\examples{
operator_weights(harmonic_operator())
round(operator_weights(operator_resolve(harmonic_operator(), 0, 365)), 8)

# an operator given a period keeps it, whatever the interval is
round(operator_weights(operator_resolve(harmonic_operator(12), 0, 365)), 8)
}
\seealso{
\code{\link[=harmonic_operator]{harmonic_operator()}}, whose \code{period = NULL} this resolves.
}
